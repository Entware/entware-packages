--- a/extra/comp_err.c
+++ b/extra/comp_err.c
@@ -37,7 +37,7 @@
 #define MAX_SECTIONS 4
 #define HEADER_LENGTH 32                /* Length of header in errmsg.sys */
 #define ERRMSG_VERSION 4                /* Version number of errmsg.sys */
-#define DEFAULT_CHARSET_DIR "../sql/share/charsets"
+#define DEFAULT_CHARSET_DIR "/opt/share/charsets"
 #define ER_PREFIX "ER_"
 #define ER_PREFIX2 "MARIA_ER_"
 #define WARN_PREFIX "WARN_"
@@ -45,8 +45,8 @@ static char *OUTFILE= (char*) "errmsg.sy
 static char *HEADERFILE= (char*) "mysqld_error.h";
 static char *NAMEFILE= (char*) "mysqld_ername.h";
 static char *STATEFILE= (char*) "sql_state.h";
-static char *TXTFILE= (char*) "../sql/share/errmsg-utf8.txt";
-static char *DATADIRECTORY= (char*) "../sql/share/";
+static char *TXTFILE= (char*) "/opt/share/errmsg-utf8.txt";
+static char *DATADIRECTORY= (char*) "/opt/share/";
 #ifndef DBUG_OFF
 static char *default_dbug_option= (char*) "d:t:O,/tmp/comp_err.trace";
 #endif
--- a/sql-common/conf_to_src.c
+++ b/sql-common/conf_to_src.c
@@ -24,7 +24,7 @@
 #include <string.h>
 #include <unistd.h>
 
-#define CHARSETS_SUBDIR "sql/share/charsets"
+#define CHARSETS_SUBDIR "/opt/share/charsets"
 #define CTYPE_TABLE_SIZE      257
 #define TO_LOWER_TABLE_SIZE   256
 #define TO_UPPER_TABLE_SIZE   256
--- a/mysys/my_default.c
+++ b/mysys/my_default.c
@@ -1186,7 +1186,7 @@ static const char **init_default_directo
     {
       errors += add_directory(alloc, fname_buffer, dirs);
 
-      strncat(fname_buffer, "/data", sizeof(fname_buffer));
+      strncat(fname_buffer, "/opt/data", sizeof(fname_buffer));
       errors += add_directory(alloc, fname_buffer, dirs);
     }
   }
@@ -1197,8 +1197,8 @@ static const char **init_default_directo
   if (DEFAULT_SYSCONFDIR[0])
     errors += add_directory(alloc, DEFAULT_SYSCONFDIR, dirs);
 #else
-  errors += add_directory(alloc, "/etc/", dirs);
-  errors += add_directory(alloc, "/etc/mysql/", dirs);
+  errors += add_directory(alloc, "/opt/etc/", dirs);
+  errors += add_directory(alloc, "/opt/etc/mysql/", dirs);
 #endif /* DEFAULT_SYSCONFDIR */
 
 #endif
--- a/strings/conf_to_src.c
+++ b/strings/conf_to_src.c
@@ -480,10 +480,10 @@ main(int argc, char **argv  __attribute_
   fprintf(f, "/*\n");
   fprintf(f, "  This file was generated by the conf_to_src utility. "
           "Do not edit it directly,\n");
-  fprintf(f, "  edit the XML definitions in sql/share/charsets/ instead.\n\n");
+  fprintf(f, "  edit the XML definitions in /opt/share/charsets/ instead.\n\n");
   fprintf(f, "  To re-generate, run the following in the strings/ "
           "directory:\n");
-  fprintf(f, "    ./conf_to_src ../sql/share/charsets/ > FILE\n");
+  fprintf(f, "    ./conf_to_src /opt/share/charsets/ > FILE\n");
   fprintf(f, "*/\n\n");
   fprint_copyright(f);
   fprintf(f,"#include \"strings_def.h\"\n");
--- a/cmake/install_layout.cmake
+++ b/cmake/install_layout.cmake
@@ -88,7 +88,7 @@ IF(UNIX)
   ELSEIF(INSTALL_LAYOUT MATCHES "SVR4")
     SET(default_prefix "/opt/mysql/mysql")
   ELSE()
-    SET(default_prefix "/usr/local/mysql")
+    SET(default_prefix "/opt")
   ENDIF()
   IF(CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT)
     SET(CMAKE_INSTALL_PREFIX ${default_prefix}
@@ -110,7 +110,7 @@ SET(INSTALL_SBINDIR_STANDALONE
 SET(INSTALL_SCRIPTDIR_STANDALONE        "scripts")
 #
 SET(INSTALL_LIBDIR_STANDALONE           "lib")
-SET(INSTALL_PLUGINDIR_STANDALONE        "lib/plugin")
+SET(INSTALL_PLUGINDIR_STANDALONE        "lib/mysql")
 #
 SET(INSTALL_INCLUDEDIR_STANDALONE       "include/mysql")
 #
@@ -127,7 +127,7 @@ SET(INSTALL_SUPPORTFILESDIR_STANDALONE
 #
 SET(INSTALL_MYSQLDATADIR_STANDALONE     "data")
 
-SET(INSTALL_UNIX_ADDRDIR_STANDALONE     "/tmp/mysql.sock")
+SET(INSTALL_UNIX_ADDRDIR_STANDALONE     "/opt/var/run/mysqld.sock")
 #
 # RPM layout
 #
--- a/client/mysql_plugin.c
+++ b/client/mysql_plugin.c
@@ -580,8 +580,7 @@ static int search_paths(const char *base
   int i= 0;
 
   static const char *paths[]= {
-    "", "/share/",  "/scripts/", "/bin/", "/sbin/", "/libexec/",
-    "/mysql/", "/sql/",
+    "", "/opt/share/",  "/opt/scripts/", "/opt/bin/", "/opt/sbin/", "/opt/libexec/"
   };
   for (i = 0 ; i < (int)array_elements(paths); i++)
   {
@@ -972,9 +971,8 @@ static int find_tool(const char *tool_na
   int i= 0;
 
   const char *paths[]= {
-    opt_mysqld, opt_basedir, opt_my_print_defaults, "/usr",
-    "/usr/local/mysql", "/usr/sbin", "/usr/share", "/extra", "/extra/debug",
-    "/extra/release", "/bin", "/usr/bin", "/mysql/bin"
+    opt_mysqld, opt_basedir, opt_my_print_defaults, "/opt/bin",
+    "/opt/sbin", "/opt/usr/bin", "/opt/usr/sbin", "/opt/share"
   };
   for (i= 0; i < (int)array_elements(paths); i++)
   {
--- a/sql/tzfile.h
+++ b/sql/tzfile.h
@@ -28,7 +28,7 @@
 */
 
 #ifndef TZDIR
-#define TZDIR	"/usr/share/zoneinfo" /* Time zone object file directory */
+#define TZDIR	"/opt/share/zoneinfo" /* Time zone object file directory */
 #endif /* !defined TZDIR */
 
 /*
--- a/scripts/CMakeLists.txt
+++ b/scripts/CMakeLists.txt
@@ -170,7 +170,7 @@ ENDIF(UNIX)
 # the documentation, so better not change this.
 
 IF(INSTALL_LAYOUT MATCHES "STANDALONE")
-  SET(prefix ".")
+  SET(prefix "/opt")
   SET(bindir ${prefix}/${INSTALL_BINDIR})
   SET(sbindir ${prefix}/${INSTALL_SBINDIR})
   SET(scriptdir ${prefix}/${INSTALL_BINDIR})
