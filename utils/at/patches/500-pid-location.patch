--- a/configure
+++ b/configure
@@ -5060,11 +5060,11 @@ systemdsystemunitdir=$with_systemdsystem
 { $as_echo "$as_me:${as_lineno-$LINENO}: checking location of pid file" >&5
 $as_echo_n "checking location of pid file... " >&6; }
 if test -d /var/run ; then
-    PIDDIR=/var/run
+    PIDDIR=/opt/var/run
 elif test -d /usr/run ; then
-    PIDDIR=/usr/run
+    PIDDIR=/opt/var/run
 else
-    PIDDIR="$ETCDIR"
+    PIDDIR=/opt/var/run
 fi
 
 cat >>confdefs.h <<_ACEOF
