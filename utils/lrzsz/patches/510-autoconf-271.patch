--- a/aclocal.m4
+++ b/aclocal.m4
@@ -117,8 +117,6 @@ dnl AM_INIT_AUTOMAKE(package,version, [n
 
 AC_DEFUN(AM_INIT_AUTOMAKE,
 [AC_REQUIRE([AC_PROG_INSTALL])
-dnl We require 2.13 because we rely on SHELL being computed by configure.
-AC_PREREQ([2.13])
 PACKAGE=[$1]
 AC_SUBST(PACKAGE)
 VERSION=[$2]
