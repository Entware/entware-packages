--- a/src/command.c
+++ b/src/command.c
@@ -181,7 +181,7 @@ static const char *cmd_lookup(const char
     search = getenv("PATH");
 
     if (!search)
-        search = "/bin:/usr/bin:/sbin:/usr/sbin";
+        search = "/opt/bin:/opt/sbin:/bin:/usr/bin:/sbin:/usr/sbin";
 
     p = search;
 
